import styled from 'styled-components';

const Bold = styled.b`
    font-family: Calibre;
    font-style: normal;
    font-weight: 600;
    font-size: 17px;
    line-height: 21px;
`;

export default Bold;
